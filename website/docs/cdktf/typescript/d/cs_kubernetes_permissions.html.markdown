---
subcategory: "Container Service for Kubernetes (ACK)"
layout: "alicloud"
page_title: "Alicloud: alicloud_cs_kubernetes_permissions"
sidebar_current: "docs-alicloud-datasource-cs-kubernetes-permissions"
description: |-
  Provides a list of Ram user permissions.
---


<!-- Please do not edit this file, it is generated. -->
# alicloud\_cs\_kubernetes\_permissions

This data source provides a list of Ram user permissions.

-> **NOTE:** Available in v1.122.0+.

## Example Usage

```typescript
import * as constructs from "constructs";
import * as cdktf from "cdktf";
/*Provider bindings are generated by running cdktf get.
See https://cdk.tf/provider-generation for more details.*/
import * as alicloud from "./.gen/providers/alicloud";
class MyConvertedCode extends cdktf.TerraformStack {
  constructor(scope: constructs.Construct, name: string) {
    super(scope, name);
    const dataAlicloudRamUsersUsersDs =
      new alicloud.dataAlicloudRamUsers.DataAlicloudRamUsers(this, "users_ds", {
        nameRegex: "your_user_name",
      });
    const dataAlicloudCsKubernetesPermissionsDefault =
      new alicloud.dataAlicloudCsKubernetesPermissions.DataAlicloudCsKubernetesPermissions(
        this,
        "default",
        {
          uid: cdktf.Token.asString(
            cdktf.propertyAccess(dataAlicloudRamUsersUsersDs.users, ["0", "id"])
          ),
        }
      );
    new cdktf.TerraformOutput(this, "permissions", {
      value: dataAlicloudCsKubernetesPermissionsDefault.permissions,
    });
  }
}

```

## Argument Reference

The following arguments are supported.
* `uid` - (Required) The ID of the RAM user. If you want to query the permissions of a RAM role, specify the ID of the RAM role.

## Attributes Reference

* `id` - Resource ID.
* `uid` - The ID of the RAM user. If you want to query the permissions of a RAM role, specify the ID of the RAM role.
* `permissions` - A list of user permission.
  * `resourceId` - The permission settings to manage ACK clusters. 
  * `resourceType` - The authorization type. Valid values `cluster`, `namespace` and `console`.
  * `roleName` - The name of the predefined role. If a custom role is assigned, the value is the name of the assigined custom role.
  * `roleType` - The predefined role. Valid values `admin`,`ops`,`dev`,`restricted` and `custom`.
  * `isOwner` - ndicates whether the permissions are granted to the cluster owner. Valid values `0`, `1`.
  * `isRamRole` -Indicates whether the permissions are granted to the RAM role. Valid values `0`,`1`.

<!-- cache-key: cdktf-0.17.0-pre.15 input-d13dd756c3f82c4619d35f996792c3f0d87450285790f288ebd433e605c4fced -->